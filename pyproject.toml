[project]
name = "backend-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "ollama>=0.4.7",
    "ruff>=0.11.5",
]
